# Cricket game assets — stylised batsman-POV kit

Procedural Blender generators plus Godot 4 shaders and a lighting rig, aimed at
the bright, saturated, soft-shadowed stadium look of the reference image.

Everything here is generated by script. There are no binary source assets to
lose: change a number, re-run, get new `.glb` files.

---

## The one thing to understand first

**This look is about 80% lighting and 20% models.** A rough model under the rig
in `build_lookdev.py` reads correctly; a beautiful model under default engine
lighting looks like a 2012 sports game. Build the lighting first, with grey
boxes, and only then model.

The single most important light is not the sun. It is **`GrassBounce`** — a dim
*green* light shining *upward*. Outdoors on grass, every downward-facing
surface (under the trousers, the pads, the jaw) picks up reflected green.
Without it, white kit goes dead grey in shadow and the scene reads as a model
in a void instead of a player on a field.

---

## Layout

```
blender/
  lib/meshutil.py       bevel, loft, swept tubes, arcs, UV helpers
  lib/mat.py            the palette + material builders
  build_ground.py       outfield, pitch, creases, stumps, boundary rope
  build_stadium.py      stand module, seat, crowd card, hoarding, sightscreen, floodlight
  build_viewmodel.py    bat, gloves, forearms, helmet grille (first person)
  build_lookdev.py      sun, skylight, grass bounce, stand fill, sky, cameras
  generate_textures.py  crowd atlas, hoardings, signage (Pillow, no painting)
  render_sheet.py       each asset alone under the real rig
  build_all.py          builds everything, exports glTF, renders previews
godot/
  shaders/mown_grass.gdshader       world-space mowing stripes, analytically AA'd
  shaders/stylized_fabric.gdshader  cricket whites with sheen
  shaders/crowd_card.gdshader       billboarded, atlased, de-synchronised crowd
  scenes/lookdev.tscn               the lighting rig, ported with matching numbers
  scripts/stadium_builder.gd        MultiMesh instancing of the bowl
  scripts/batsman_pov.gd            POV camera + viewmodel attachment
exports/    generated .glb files
renders/    generated preview stills
```

## Running it

```bash
pip install bpy pillow     # Blender 5.0 as a Python module; needs Python 3.11
cd blender
python3 generate_textures.py              # run once; regenerates all PNGs
python3 build_all.py --no-render          # export only, a few seconds
python3 build_all.py --samples 64 --res 1600 900
```

Textures must exist before the builders run — materials fall back to flat grey
if `textures/` is empty.

Headless rendering needs a GL context:

```bash
apt-get install -y xvfb libegl1 libgl1-mesa-dri libglx-mesa0
LIBGL_ALWAYS_SOFTWARE=1 GALLIUM_DRIVER=llvmpipe \
  xvfb-run -a -s "-screen 0 1920x1080x24" python3 build_all.py
```

Without a GL context EEVEE **does not error** — it silently writes a blank
white frame. If your renders come out white, that is why.

---

## The lighting numbers

Measured on this scene, not guessed. Blender values are in `build_lookdev.py`;
the Godot column is what `scenes/lookdev.tscn` actually sets.

| | Blender | Godot |
|---|---|---|
| Sun elevation / azimuth | 41° / 128° | `rotation_degrees (-41, -142, 0)`, `rotation_order = 2` |
| Sun colour | `(1.0, 0.964, 0.894)` ≈5800 K | same |
| Sun energy | `4.0` W/m² | `light_energy = 1.6` |
| Sun softness | `angle = 2.5°` | `light_angular_distance = 2.5` |
| Sky / IBL | Background strength `0.30` | `ambient_light_energy = 0.3`, source = Sky |
| Grass bounce | Sun, `(0.35,0.55,0.20)`, energy `0.8`, pointing up | `light_energy = 0.3`, `rotation_degrees (90,0,0)` |
| Exposure | view transform exposure `-2.0` | `tonemap_exposure = 1.0` |
| Tonemap | **Khronos PBR Neutral** | Filmic + `adjustment_saturation = 1.15` |
| AO | — | `ssao_radius = 1.2`, `ssao_intensity = 0.4` |
| Bloom | (see note) | `glow_intensity 0.42`, `glow_hdr_threshold 1.0` |
| DOF | f/4.0, focus 18 m | far blur from 18 m, **near blur off** |

`rotation_order = 2` (YXZ) is load-bearing. Godot's inspector default is YXZ
but the `.tscn` default is XYZ, and with the wrong order this exact sun points
*upward*. Verified: the Godot sun direction is `(0.465, -0.656, 0.595)`, which
matches the Blender rig to three decimals.

### Why not AgX

AgX is the fashionable choice and it is wrong for this look. Measured side by
side on this scene, AgX held the outfield at **0.31 chroma** while Khronos PBR
Neutral held **0.45**. AgX is built to roll film highlights off gracefully, and
it eats exactly the saturated mid-tones this art style is made of. Godot has no
PBR Neutral, so `lookdev.tscn` uses Filmic and puts the saturation back with
`adjustment_saturation`.

### Why the sky is at 0.30

A full-strength physical sky washes the sun out and everything goes pastel. At
0.30 the key light dominates and the grass keeps its colour. This is an
artistic ratio, not a physical one.

### Bloom

`build_lookdev.py` has `_add_bloom()`, and it is **off by default**. On Blender
5.0.1 built as the `bpy` module, assigning any `compositing_node_group` — even
a bare input→output passthrough — makes `render()` emit a black frame in ~0.5 s
without rasterising at all. It needs a real GL context that headless bpy does
not provide, and it fails silently. Inside the Blender GUI it works. Bloom is
Godot's job anyway; `lookdev.tscn` sets the equivalent glow.

---

## Colour space

Palette values in `lib/mat.py` are written as **sRGB** — the numbers you would
read off a colour picker — and converted to linear by `_srgb()` on the way into
the shader.

Getting this backwards is the classic renderer bug and it is not subtle. Feed
sRGB `0.78` straight into a linear socket and a tan pitch renders pale grey and
a rich outfield renders mint. **If a surface looks washed out, suspect this
before you touch the lights.**

## Roughness table

Roughness carries the material story; albedo stays nearly flat.

| Surface | Roughness | Notes |
|---|---|---|
| Cotton twill kit | 0.80 | + sheen/RIM 0.32 |
| Pad & glove leather | 0.45 | |
| Helmet shell | 0.25 | |
| Helmet grille | 0.30 | metallic 1.0 |
| Bat willow | 0.35 | + clearcoat for the lacquer |
| Bat grip rubber | 0.65 | |
| Outfield grass | 0.48 / 0.72 | the two mowing directions |
| Pitch | 0.90 | |
| Concrete | 0.85 | |

Whites sit at **0.88, never 1.0**. Pure white clips, and a clipped surface can
no longer pick up the green bounce — which is the whole point.

---

## Matching the reference photograph

The stadium was rebuilt to the structure of the reference rather than a
generic bowl:

| Reference feature | How it is built |
|---|---|
| Two seating tiers | `L_ROWS` 14 rows at R80–90, `U_ROWS` 13 rows at R92–103 |
| Glazed corporate-box balcony between them | `Box_Module` + `Box_Glass`, recessed at R91.1 with precast piers |
| Green signage band between tiers | `Signage_Module`, textured, positionally UV'd |
| Green board standing above the roofline | `RoofSign_Module`, on 2 of every 3 segments |
| Halogen pylons in frame | 6 towers at 15°+60°k, 5×2 lamp array at emission 220 |
| Dark canopy roof | `Roof_Module`, deliberately shallow (see below) |
| Dense colourful crowd | 4×4 atlas, 16 figures, 94% seat occupancy |
| Branded boundary boards | 6 invented brands, cycled round the boundary |
| Floodlight pylons | wide lamp head, emissive, bloom does the glare |
| Trees on the skyline | 46 stylised trees at R112 |
| Big cumulus sky | procedural, projected onto a cloud plane |
| Mown outfield stripes | shader, world-space, analytically antialiased |

The balcony matters more than it sounds. The reference does not stack two
identical decks — it separates them with a recessed, glazed box level, and
that dark horizontal band is most of what makes the bowl read as a real
stadium rather than two rings of terracing. Same for the roofline: a
continuous ring of boards looks like a wall, so they go on two segments in
every three.

### Four mistakes worth not repeating

**The canopy made the stands read as a black void.** A tall back wall plus a
deep soffit put a solid dark band across every stand. Real roofs read as a
thin edge from pitch level. `ROOF_REACH` dropped 8 m → 4 m and the back wall
3.2 m → 1.6 m. A stadium bowl also bounces enormous light around its own
interior, which no key-plus-sky rig reproduces — hence `StandFill`, a
shadowless cool light standing in for interreflection.

**Cloud ramp WIDTH is the whole game.** The density ramp's two stops sit
0.055 apart, and that narrowness is what gives cumulus their defined edges.
At 0.18 apart the identical noise renders as flat grey haze at *every* camera
elevation — it reads as pollution, not weather. No amount of brightness,
coverage or cloud size rescues it, because the edges are the cloud. Two
tempting wrong turns on the way here: raising coverage (made it more overcast)
and enlarging the cells (made the noise so low-frequency it went smooth). Move
the two stops *together* to change coverage; keep the gap tight.

**Cloud "shadow" colour turned a sunny sky overcast.** Raising cloud coverage
to match the reference exposed a base colour of 2.6 radiance, which at high
coverage reads as storm cloud. Sunlit cumulus have *bright* bases; it is now
5.0. Coverage and base brightness have to move together.

**`recalc_face_normals` scrambles index-assigned UVs.** It can reverse a
face's winding, which reorders its loops. The signage printed back to front,
and flipping U during construction changed nothing because the reversal
happened afterwards. Any text-bearing surface is now mapped from **world
position** after the recalc, never from loop order.

**Every floodlight rendered as a black silhouette.** The lamps sit on the
head's local +Y, which after a Z-rotation of θ points along θ+90. Aiming them
at the middle of the ground (direction a+180) needs a rotation of **a+90**,
not a+180 — the intuitive value put the whole array 90° off, with its emissive
faces pointing at the car park. Worth checking on anything whose front face
matters: it renders perfectly, just facing the wrong way.

A fifth, less subtle one: the first reference render aimed the camera at the
*shadowed* half of the bowl. With a key at azimuth 128°, the lit stands are on
the −Y arc. Check which side the sun is on before blaming the lighting.

## Verified import

All nine exports round-trip into Godot 4.3 with material names intact — those
names are the contract the engine keys on when swapping in the custom shaders.

| File | Meshes | Tris | Materials |
|---|---|---|---|
| `ground.glb` | 6 | 13,712 | GrassMown, Pitch, Crease, Willow |
| `viewmodel.glb` | 8 | 7,428 | Willow, Grip, PadWhite, Skin, KitWhite, Grille |
| `floodlight_lamps.glb` | 1 | 1,620 | Floodlight |
| `stand_module.glb` | 1 | 564 | Concrete |
| `sightscreen.glb` | 1 | 324 | Sightscreen, SteelDark |
| `seat.glb` | 1 | 216 | Seat |
| `floodlight_tower.glb` | 1 | 144 | SteelDark |
| `hoarding_panel.glb` | 1 | 108 | Hoarding |
| `crowdcard.glb` | 1 | 2 | Crowd |

**24,118 triangles for the entire unique asset set.** The bowl is built from
these by instancing, so the stadium costs draw calls, not memory.

## Asset notes

**Real dimensions throughout.** Pitch 20.12 m stump to stump, 3.05 m wide,
popping crease 1.22 m in front, stumps 0.711 m, boundary 68 m. A batsman-POV
camera sits 1.65 m up and players read wrong scale instantly.

**The outfield is crowned** — 0.45 m higher at the centre, like a real ground.
`build_ground.ground_height()` is the single source of truth for that surface
and is duplicated in `stadium_builder.gd`; keep the two in sync.

**Stadium modules are exported one at a time, at the origin**, for
MultiMesh instancing. A full ground is ~28,000 seats — as individual nodes that
is unshippable, as one MultiMesh it is one draw call. `assemble_bowl()` in
`build_stadium.py` builds the whole thing only for preview renders, and
`stadium_builder.gd` places the same modules at the same transforms.

**The viewmodel is the priority asset.** In batsman POV the player never sees a
full character — they see the bat, gloves, forearms and grille, 0.5 m from the
near plane, every frame. Budget detail accordingly. The gloves are built in
cylindrical coordinates *about the handle axis*, so the fingers are in contact
with the bat by construction and cannot drift off it when you retune the pose.

**Mowing stripes are not paint.** They are the same grass mown in opposite
directions, so the albedo difference is tiny (~6%) and roughness does the work.
Push the colours apart instead and it reads as a painted pitch. The Godot
shader antialiases them with `fwidth()`; without that they turn to moiré about
40 m out, which is very visible from a batsman's eye height.

---

## Tuning the viewmodel stance

`build_viewmodel.attach_to_camera()` takes `offset`, `tilt_deg`, `roll_deg`,
`yaw_deg`. The defaults were found by rendering a sweep and looking at the
results, not by reasoning — the interaction of three rotations with a 26 mm
lens is not intuitive. Do the same if you retune. `batsman_pov.gd` carries the
identical constants; change both or the stills and the game will disagree.

Note that Godot's `Camera3D.fov` is **vertical**. A 26 mm full-frame lens is
~69° horizontal, which is ~46° vertical at 16:9. Setting 69 there is the
standard mistake and gives a visibly fish-eyed pitch.

---

## Before you ship

The reference image contains a real player's name and number and real sponsor
trademarks (a bat brand, tile and motorcycle logos). The **art style** is free
to copy. The likeness and the logos are not, and they will block a store
release. Everything here uses generic kit and placeholder hoardings — keep it
that way and invent your own brands.

## Known gaps

Visible in `renders/pov.png`, listed honestly so you know what is unfinished
rather than broken:

- **The stand interior reads near-black.** The roof cantilever shades the upper
  tier and nothing fills it. Real grounds bounce a lot of light around inside
  the bowl. Fix by raising `ambient_light_energy` for the stadium specifically,
  shortening the roof in `build_stadium.build_stand_module()`, or adding a dim
  fill light aimed into the stands. This is the most visible remaining issue.
- **The crowd is untextured**, so cards read as dark blocks. The 4×4 atlas that
  `crowd_card.gdshader` expects has not been authored. The shader's tint and
  bob variation cannot do their job until it exists.
- **The gloves do not read as hands.** This is the worst asset in the set and
  `renders/asset_02_glove.png` shows it plainly: four finger tubes wrapped as
  full 245° arcs around the handle stack up into something like a bedspring,
  with the cuff as a mushroom cap on top. Wrapping in cylindrical coordinates
  did fix the earlier problem (fingers no longer float off the bat) but it
  overcorrected — real fingers wrap maybe 180°, are not concentric, and the
  back of the hand should be a single broad mass that hides the roll ends.
  This one needs sculpting rather than another parameter tweak.
- **The bat presents edge-on** in the default stance, so the face is hidden.
  Adjust `yaw_deg` in `attach_to_camera()` / `VM_YAW_DEG` if you want more face.
- **Forearms are bare skin cylinders** — plausible in silhouette, crude up
  close, and they are 0.5 m from the camera. Long sleeves would hide most of
  this cheaply.
- **Hoarding brand text is not legible at true distance.** The taglines read;
  the brand line needs larger type in `generate_textures.hoarding()` if you
  want it readable from the middle of the ground.
- **The stand module is hard to read in isolation** — the roof slab dominates
  and the terracing behind it is barely visible. Fine at stadium distance,
  but it is the piece most worth revisiting if you ever show the stands close.
- No textures anywhere. Materials are flat colour + roughness; UVs are
  unwrapped and ready for painting.
- No character rig. Bowler and fielders are not built — in POV they are distant
  and low-poly, so they are the next job, not the first.
- **`Cam_Hero` is framed too low** — `renders/hero.png` is mostly ground with no
  sky. The depth of field and the creases read nicely, but the framing needs
  raising and tilting up in `build_lookdev.add_camera_hero()`. It is a preview
  camera only; nothing in the game depends on it.
- No animation, no ball, no physics.
- `lookdev.tscn` is verified to load correctly in Godot 4.3 headless, but has
  **not been rendered in Godot** — there is no GPU in the environment it was
  built in. Expect to adjust `light_energy` and `adjustment_saturation` by eye
  once you see it running.

### Suggested order of work from here

1. **Rebuild the gloves.** They are on screen every frame at half a metre and
   they are currently the weakest thing in the project.
2. Fill light in the stands (one number, biggest visual win for the environment).
3. Crowd atlas — 16 spectator cutouts, 1024², alpha.
4. Textures for the kit and pads; the UVs are already there.
5. Bowler and fielder low-poly characters.
6. Ball, physics and shot animation.

## Inspecting assets individually

`render_sheet.py` renders each asset alone on a neutral pad under the real
lighting rig, framed automatically from its bounding box:

```bash
LIBGL_ALWAYS_SOFTWARE=1 GALLIUM_DRIVER=llvmpipe \
  xvfb-run -a -s "-screen 0 1920x1080x24" python3 render_sheet.py
```

Scene shots hide weak geometry behind distance and depth of field — the glove
problem above was invisible in `pov.png` and obvious the moment the asset was
rendered on its own. Do this before trusting any asset.

Note the gotcha that script documents: **`matrix_world` is cached**, and
reading it right after setting `obj.location` from Python returns the stale
transform. That silently mis-framed the bat and buried its blade under the
shadow pad. Call `bpy.context.view_layer.update()` before any world-space read.
